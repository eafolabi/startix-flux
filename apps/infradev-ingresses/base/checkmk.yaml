---
apiVersion: v1
kind: Service
metadata:
  name: checkmk
  namespace: noc
spec:
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: 8080
  type: ClusterIP
---
apiVersion: v1
kind: Endpoints
metadata:
  name: checkmk
  namespace: noc
subsets:
  - addresses:
      - ip: ${infradev_librenms_ip}
    ports:
      - port: 8080
        protocol: TCP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    nginx.ingress.kubernetes.io/auth-signin: https://oauth.${environment}.b2.${base_domain}/oauth2/start
    nginx.ingress.kubernetes.io/auth-url: http://oauth2-proxy.ingress.svc.cluster.local/oauth2/auth
  name: checkmk
  namespace: noc
spec:
  ingressClassName: nginx
  rules:
    - host: checkmk.${environment}.b2.${base_domain}
      http:
        paths:
          - backend:
              service:
                name: checkmk
                port:
                  number: 8080
            path: /
            pathType: Prefix
  tls:
    - hosts:
        - checkmk.${environment}.b2.${base_domain}
